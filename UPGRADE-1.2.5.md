# Aether Desktop 1.2.5

سه کار: تور به ویندوز آمد، هستهٔ ۲.۰.۰ نشست، و یک نشتِ نامِ دامنه که در بازرسیِ
همین ارتقا پیدا شد بسته شد.

## ۱) تور — پورت از `AetherMobile 1.3.0`

چهار بک‌اندِ تازه در «پیشرفته ← بک‌اند»، همان چهارتای موبایل و با همان نگاشت روی
فلگ‌های خودِ هسته:

| بک‌اند | فلگِ هسته | خروجی | تونل زیرِ تور |
|---|---|---|---|
| `Tor` | `--tor-only` | تور | ندارد |
| `Aether → Tor` | `--tor` | تور | دارد (WARP) |
| `Tor → Aether` | `--tor-reverse` | Aether | تور اول است |
| `Tor → Psiphon` | `--tor-only` + استیج ۲ | سایفون | تور اول است |

### چرا لایهٔ `TorSocksFront` موبایل در ویندوز لازم نبود

در اندروید ترافیکِ برنامه از یک `VpnService` می‌گذرد و برای اینکه سوکت‌های خودِ
تور از آن تونل رد نشوند یک لایهٔ SOCKS جدا لازم است. در ویندوز مسیرِ داده از
همان پروکسیِ محلی می‌گذرد که برنامه خودش بالا آورده، پس تنها چیزی که لازم بود
انتخابِ **درگاهِ درست** بود: در `TorMode::Only` خروجی همان `LOCAL_SOCKS_PORT`
است، و در `Chain`/`Reverse` باید `TOR_SOCKS_PORT` باشد. اگر این یک تصمیم اشتباه
می‌شد، کاربری که `Aether → Tor` را انتخاب کرده بی هیچ خطایی از خروجیِ WARP بیرون
می‌رفت — یعنی بدترین شکلِ شکست: تونل سبز، تور غایب.

### پل‌ها فقط جایی که معنا دارند

`TorBridges` سه حالت دارد (`Auto`، `Always`، `Off`) و **فقط** در `Tor` و
`Tor → Psiphon` فعال است. در `Aether → Tor` تور از داخلِ تونل شماره‌گیری می‌شود و
شبکه‌ای که تور می‌بیند شبکهٔ اپراتور نیست؛ پس پل بی‌اثر است و رابط کاربری همین را
می‌گوید و کنترل را خاکستری می‌کند — یک کنترلِ فعال که هیچ کاری نمی‌کند، از یک
کنترلِ غیرفعالِ توضیح‌دار بدتر است.

`Tor → Aether` یک محدودیتِ اضافه دارد که انتخابِ این برنامه نیست: تور در این حالت
باید پیش از بالا آمدنِ تونل به شبکه برسد، پس پل هم از شبکهٔ اپراتور می‌گذرد.

### درصدِ راه‌اندازی، ترجمه‌شده

راه‌اندازیِ تور کند است و کاربر باید بداند «دارد کار می‌کند» یا «گیر کرده». پس
`tor_bootstrap.rs` درصد را از لاگِ موتور می‌خواند و سه چیز را از هم جدا می‌کند:
`TorGate::Ready`، `Waiting` (درصد بالا می‌رود) و `Stalled` (بودجهٔ زمانی تمام شد
بی پیشرفت).

نکتهٔ ریزی که ارزش نوشتن دارد: درصد **در خودِ رشتهٔ وضعیت جاسازی نمی‌شود**. اول
همین کار شده بود و نتیجه‌اش این بود که خطِ وضعیت انگلیسیِ خام می‌شد و در رابطِ
فارسی هم انگلیسی می‌ماند. حالا `Snapshot` یک فیلدِ عددیِ جدا دارد
(`tor_percent: Option<u8>`) و `home.js` جمله را با `t()` می‌سازد. آزمونِ
`status_test.mjs` نُه حالت را در دو زبان می‌سنجد، و رشته‌ها را از خودِ فایلِ
منتشرشده می‌خواند تا آزمون نسخهٔ خودش را نسنجد.

یک متن هنوز عمداً ترجمه‌ناپذیر است: پیامِ شکستِ `Tor stopped at X%` که از
`stage_failure_message` می‌آید و مقصدش لاگِ تشخیص است، نه چشمِ کاربر.

### اتصال دیگر پیش از موعد شکست نمی‌خورد

خودآزمونِ پس از اتصال، پیش از آمادگیِ شبکه اجرا می‌شد و مرحلهٔ تور را رد
می‌کرد — تور تازه در ۴۰٪ بود که برنامه نتیجه می‌گرفت شکست خورده. دروازهٔ
`TorGate` حالا میانِ این دو می‌نشیند.

و در نردبانِ Smart Auto، حالت‌های تور مثل پلهٔ تونل ردیف نمی‌شوند: نردبان برای
یافتنِ سریع‌ترین ترابرد است و یک راه‌اندازیِ ۹۰ ثانیه‌ای تورِ سالم را «کُند» و
حذف‌شدنی نشان می‌داد.

## ۲) ترابرِ افزونه (lyrebird) — پل‌ها بی آن کار نمی‌کنند

`obfs4` بی یک اجرایی از lyrebird اجرا نمی‌شود، پس `scripts/build-pt.ps1` اضافه
شد: پینِ `lyrebird-0.6.1`، بیلد با `CGO_ENABLED=0` برای ویندوز، خروجی در
`dist-engine/pt/lyrebird.exe` که `stage-payload.ps1` در `engine/pt/` بسته
می‌گذارد.

**شکستِ این مرحله بیلد را نمی‌کشد، ولی انتشار را می‌کشد.** نبودِ Go، نرسیدنِ
کلون، شکستِ `go build` — هر سه با `exit 0` و یک پیامِ روشن رد می‌شوند و مرحله در
CI `continue-on-error: true` است، عیناً مثل نسخهٔ موبایل. پل‌ها یک افزونه‌اند؛ تورِ
مستقیم و `Aether → Tor` بی آن‌ها کار می‌کنند.

ولی همین `continue-on-error` یک شکافِ انتشار داشت: بسته می‌توانست با `engine/pt/`ِ
**موجود ولی خالی** منتشر شود و کاربر بی هیچ نشانه‌ای پلِ obfs4 نداشته باشد. پس در
`build.yml` پس از `Stage payload` و پیش از ساختِ ZIP یک دروازه نشسته که فقط در
بیلدِ منتشرشدنی (نه pull-request) اجرا می‌شود: اگر
`dist/payload-*/engine/pt/lyrebird.exe` نباشد یا کوچک‌تر از ۱ مگابایت باشد،
انتشار با `exit 1` می‌ایستد.

دو تلهٔ خاموش هم در `engine.rs` بسته شد، چون بی آن‌ها بسته درست ساخته می‌شد و
پل‌ها همچنان کار نمی‌کردند:

* `prepare_runtime_engine` فقط فایل‌ها را کپی می‌کرد و زیرپوشه‌ها را نه، پس
  `engine/pt` هرگز به پوشهٔ اجرا نمی‌رسید؛
* `AETHER_TOR_PT_DIR` به `<app-data>/pt` اشاره می‌کرد و هستهٔ ۲.۰.۰ در
  `<app-data>/engine/pt` می‌گردد. حالا هر دو مسیرِ موجود با `;` فرستاده می‌شوند، و
  اگر هیچ‌کدام نبود همین در لاگ نوشته می‌شود.

## ۳) اصلاح: پرسشِ کشور، بی‌رمز و بیرون از تونل

در بازرسیِ همین ارتقا پیدا شد. برای دقیق‌کردنِ کشورِ خروج یک درخواستِ
`http://ip-api.com` — **بی TLS** — فرستاده می‌شد. برای ابزاری که کاربرش زیر
نظارت است این هم نامِ دامنه را روی سیم می‌گذارد و هم پاسخ را دستکاری‌پذیر
می‌کند.

حالا این پرسش فقط از **داخلِ تونلِ برقرار** می‌رود و بی تونل هیچ درخواستی
نمی‌رود؛ کشور در آن حالت از خودِ اندپوینت خوانده می‌شود، همان چیزی که پیش از این
هم نمایش داده می‌شد.

## ۴) هستهٔ ۱.۹.۰ → ۲.۰.۰

merge سه‌طرفهٔ واقعی، مثل ارتقای قبلی: baselineِ ثبت‌شدهٔ ۱.۹.۰، نسخهٔ پچ‌خوردهٔ
دسکتاپ، و آپ‌استریمِ ۲.۰.۰. پچ‌های علامت‌دارِ `AETHER-APP-PATCH` سرِ جایشان ماندند.

`CoreCaps::for_version` با `>=` مقایسه می‌کند، پس قابلیت‌های تور با رسیدنِ ۲.۰.۰
خودکار روشن می‌شوند و هیچ نسخه‌ای در کد سخت‌کد نیست؛ `CORE_VERSION` در زمانِ اجرا
از کنارِ `aether.exe` خوانده می‌شود. اگر کسی هستهٔ قدیمی‌تری بگذارد، بک‌اندهای تور
در رابط خاکستری می‌شوند و دلیلش را می‌گویند — نه اینکه انتخاب شوند و بی‌صدا شکست
بخورند.

## چه چیزی عوض **نشد**

پروفایل‌های ذخیره‌شده دست‌نخورده بارگذاری می‌شوند: بک‌اندِ پیش‌فرض همان `Aether`
است و هیچ تنظیمِ موجودی پیش‌فرض، نام یا معنایش را عوض نکرده. هیچ فلگ یا متغیرِ
محیطیِ موتور حذف نشده. لایهٔ هوش مصنوعی همان مرزهای ۱.۲.۴ را دارد و بک‌اندهای تور
از دلِ چت نوشتنی **نیستند** — `backend` از قبل بیرونِ `chat_writable()` بود و
بیرون ماند.

## آزمون‌ها

- **۷۲ آزمون واحد** روی منطقِ Rust در یک هارنس که خودِ فایل‌های مخزن را با
  `#[path]` می‌خواند (نه کپی‌شان): نگاشتِ بک‌اند به فلگ و درگاه، فعال‌بودنِ پل فقط
  در دو حالت، خواندنِ درصد و تشخیصِ `Stalled` از لاگ، و ردیف‌نشدنِ تور در نردبان.
- **۱۴۸ سنجهٔ jsdom** (`npm test`): شش بک‌اند در دو زبان رندر می‌شوند، دروازهٔ
  قابلیت‌ها با هستهٔ قدیمی کنترل را خاکستری می‌کند، و پروفایل یک مرجع دارد.
- **`status_test.mjs`** — نُه حالتِ وضعیت × دو زبان، با `captionFor` از فایلِ
  منتشرشده.
- **`check_i18n.js`** — صفر رشتهٔ ترجمه‌نشده. این ابزار خودش یک اصلاح خورد: پیش از
  این هر دو سبکِ گیومه را نمی‌خواند و رشته‌های موجود را «غایب» گزارش می‌کرد، پس
  حالا با baselineِ واقعیِ ۱.۲.۴ و یک آزمونِ منفی سنجیده می‌شود.
- **`tests/layout-desktop.mjs`** — بستهٔ واقعیِ vite در مرورگرِ واقعی، دو اندازه ×
  دو زبان: سند اسکرول ندارد و کارت کامل داخل کادر است. در کوچک‌ترین پنجرهٔ مجاز
  (۹۶۰×۶۴۰) با رابطِ فارسی **۱۹ پیکسل** فضای خالی زیرِ کارت می‌ماند؛ تنگ است، ولی
  اندازه‌گیری‌شده و نه حدس.
- **`cargo check --target x86_64-pc-windows-gnu`** بی‌خطا، و هر دو اسکریپتِ
  PowerShell هم parse و هم **اجرا** شدند: بی Go، بی شبکه، و مسیرِ موفق با
  زنجیرهٔ ابزارِ بدلی. همین اجرا یک باگ داد: بیرون از راناکِ CI متغیرِ
  `RUNNER_TEMP` وجود ندارد و مسیرِ موقت خالی می‌شد.

## آنچه در این محیط سنجیده **نشد**

باینریِ ویندوزی ساخته و لینک نشده و روی ویندوز اجرا نشده؛ lyrebirdِ واقعی
کامپایل نشده (Go در محیطِ کار نبود)؛ و راه‌اندازیِ واقعیِ تور، پل‌ها و آزمونِ نشت
باید مثل هر انتشارِ دیگر روی خودِ ویندوز انجام شود.

## بازرسی پیش از انتشار

`cargo test` هستهٔ merge‌شده، آزمون‌های رابط، بیلد وب، `cargo check` هدفِ ویندوز،
بیلد ریلیزِ Tauri روی x64 و x86، verify مانیفستِ جاسازی‌شده، نصب و حذفِ بی‌صدا،
پاک‌سازیِ کلیدِ قطع، و آزمونِ دستیِ نشتِ WebRTC/DNS/IPv6 — این بار با هر چهار
بک‌اندِ تور.

---

## ۱۷ سپتامبر — چهار اصلاح از لاگ‌ها و تصویرهای میدانی (نسخه دست‌نخورده: ۱.۲.۵)

نگهبان: `scripts/check-connection-card.py` + ۱۶ کنترلِ منفی در
`scripts/check-connection-card-negative.sh`. هر دو در `preflight-local.sh`
(بندهای ۱۷ و ۱۸) و در CI.

| موضوع | شاهد | علت | اصلاح |
|---|---|---|---|
| `Aether → Tor` و `Tor → Aether` هرگز بالا نمی‌آمدند | `loge3`: چهار پانیکِ `Cannot start a runtime from within a runtime` در `tokio-rt-worker` | `PreferredRuntime::create()` در بافتِ async؛ `tor_rtcompat 0.46.0` در `create_runtime()` یک `block_on` دارد | رانتایم روی تردِ `tor-runtime-birth` ساخته و با `oneshot` برگردانده می‌شود؛ کلاینت با `create_unbootstrapped_async()` |
| `Tor → Psiphon` پس از ۱۰۰٪ سکوت می‌کرد | `loge4`: «Tor is ready…» و سپس هیچ؛ در کلِ فایل یک «Starting Psiphon» نیست | دروازه منتظرِ پورتی بود که فقط استیج ۲ بازش می‌کند، و استیج ۲ فقط پس از دروازه شروع می‌شود | دروازه از `tor_listener_port()` می‌پرسد — پورتی که خودِ حامل باز می‌کند |
| کارت: `SMART`، کرهٔ خالی، اندپوینتِ تکراری، `397 ms` = `Poor` | تصویرهای حالتِ «تور تنها» | برچسب فقط با `chain_exit_port`؛ `loc=T1` به‌عنوان کشور؛ اندپوینت = IPِ خروجی؛ مقیاسِ پینگِ تک‌هاپی | برچسبِ خط‌لوله برای هر بک‌اندِ نام‌دار؛ `is_real_country` (رد کردنِ `T1/A1/A2/O1/XX/ZZ`) در سه نقطه؛ ماژولِ `firsthop` (هاپِ اول با رجوع به `SocketAddr` و رَدِ `192.0.2.0/24`)؛ باندهای توری `250/450/800` |
| ۳۴ ثانیه تا Connected | `loge1`: ۱۰.۷ ثانیه سکوت میان `DNS+HTTP … OK` و `WebRTC / UDP leak check` | چهار کارگزارِ STUN نوبتی، هر یک ۲.۵ ثانیه، و در حالتِ سالم هیچ‌کدام جواب نمی‌دهد | هر چهار هم‌زمان با `mpsc` + `recv_timeout`؛ پوشش عوض نشده، ~۸ ثانیه کمتر |

**سنجیده نشد:** کامپایلِ Rust (در محیطِ کار `cargo` نبود) و اجرای واقعی روی
ویندوز. باقیِ سهمِ کندی (fingerprint ~۴.۶s، گاردِ نشتی ~۶.۱s، wintun ~۷s)
دست‌نخورده است.

### همان روز، بیلدِ CI افتاد — و نگهبانی که نبود

`Build x64` و `Build x86` هر دو در **اولین** گامِ cargo افتادند (`cargo fmt --all`،
۰۷:۵۹) و دوباره در `cargo test` (۰۸:۰۷). یک علت برای هر چهار خطا:

```text
error: unexpected closing delimiter: `}`
    --> src\tor_native.rs:1340:1
1213 | impl ControlSession {
     |                     - this delimiter might not be properly closed...
Error writing files: failed to resolve mod `tor_native`
```

پچِ متنیِ من کامنتِ مستند و امضای تابعِ بعدی را روی یک سطر چسبانده بود، پس
`fn socks_port` داخلِ کامنت افتاد و آکولادِ بازش گم شد.

**جابِ Preflight سبز بود.** هر ۱۸ نگهبانِ آن وقت با `grep` و regex کار می‌کرد و
برای آن‌ها یک فایلِ نامتعادل بی‌عیب است. از این‌رو:

| افزوده | چه می‌کند |
|---|---|
| `scripts/check-rust-parses.py` | تعادلِ `{} () []` در هر ۶۸ فایلِ `.rs` با ردکردنِ رشتهٔ خام/معمولی، کاراکتر، lifetime و کامنتِ تودرتو؛ به‌علاوهٔ کشفِ «کد پس از کامنت روی یک سطر». بی‌نیاز از toolchain |
| `scripts/check-rust-parses-negative.sh` | چهار آسیب (شاملِ بازتولیدِ عینِ خطای بالا) باید قرمز شود، و یک کنترلِ **مثبت**: آکولادِ پنهان در رشتهٔ خام/کامنت نباید هشدارِ بی‌جا بدهد |
| `preflight-local.sh` بندهای ۱۹–۲۱ | نحو، کنترل‌هایش، و `cargo check --target x86_64-pc-windows-gnu --all-targets` — با ردشدنِ صریح اگر `cargo` نباشد |
| `build.yml` | هر دو نگهبان **پیش از** گام‌های cargo |

**کامپایل، این بار واقعاً:** با `rustc 1.98.1` (نسخهٔ راناکِ CI)، `mingw-w64`،
`cmake` و `libclang`: `cargo fmt --all` بی‌خطا، `cargo check --all-targets` هدفِ
`x86_64-pc-windows-gnu` با `Finished`، `cargo clippy` بی‌خطا (فقط هشدارهای
پیش‌موجود)، و کریتِ هستهٔ `aether v2.0.0` هم جدا `Finished` — که اصلاحِ
`tor-runtime-birth` را پوشش می‌دهد. برای عبورِ `build.rs` چهار فایلِ بدلی در
`dist-engine/` لازم است؛ در این بسته نیستند.
