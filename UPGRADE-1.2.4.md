# Aether Desktop 1.2.4

چهار کار، و یکی‌شان اصلاح باگی که از روز اول بود.

## ۱) دستیار هوش مصنوعی — پورت از `AetherMobile 1.2.9`

قابلیت نسخهٔ موبایل با **همان متد** به ویندوز آمد: ✨ کنار هر تنظیم، صفحهٔ
دستیار (گفت‌وگو + مشاور اتصال + مشاور تنظیمات)، و همان دروازه‌ای که وقتی چیزی
سرِ جایش نیست، *دلیلش* را می‌گوید و ساکت نمی‌ماند.

### مرزها — و چرا هر کدام هست

**کلید کاربر با DPAPI مهر می‌شود.** در اندروید کلید با AES-GCM سخت‌افزاری از
Android Keystore مهر می‌شود. معادل *واقعیِ* آن روی ویندوز `CryptProtectData`
است، نه یک هم‌سنگ سلیقه‌ای: کلید رمزنگاری از نشستِ ورودِ همین کاربر مشتق
می‌شود، هرگز در دسترس فرآیند ما نیست، و `secrets.bin` که از پروفایل بیرون
کشیده شود روی هیچ حساب یا دستگاه دیگری باز نمی‌شود. `CRYPTPROTECT_UI_FORBIDDEN`
صریح پاس داده می‌شود، چون این کد ممکن است از رشتهٔ پس‌زمینه صدا زده شود و DPAPI
هرگز نباید پنجره‌ای بالا بیاورد که کسی منتظرش نیست. انبار راز از `profile.json`
جدا است: «بازنشانی همهٔ تنظیمات» کلید را نمی‌برد.

**ترافیک هوش مصنوعی از خودِ تونل می‌رود.** سوکت‌های خودِ برنامه از تونل رد
نمی‌شوند، پس یک کلاینت HTTP معمولی روی شبکهٔ اپراتور بیرون می‌زد. درخواست‌ها از
پروکسی SOCKS5 محلی شماره‌گیری می‌شوند و نام میزبان به‌صورت `ATYP=DOMAIN`
فرستاده می‌شود تا **نقطهٔ خروج** آن را حل کند؛ resolver اپراتور نه پرسش را
می‌بیند و نه شکستش را تحویل می‌دهد.

**آنچه دستگاه را ترک می‌کند فیلتر می‌شود، نه فقط کوتاه.** لاگ اتصال را ابزاری
می‌نویسد که کاربرش زیر نظارت است: نشانی اندپوینت، آی‌پی اپراتور، شناسهٔ
ثبت‌نام Zero Trust، و هر رازی که کسی در فیلدی چسبانده باشد. پس رشته‌های شبیه
راز — از جمله کلید Gemini خودِ کاربر که وگرنه از دل درخواستی که **با** همان
کلید احراز شده بازگو می‌شد — جایگزین می‌شوند؛ IPv4 عمومی به /16 و IPv6 به /32
ماسک می‌شود (برای استدلال «همان لبه مدام می‌شکند» کافی، برای شناسایی مشترک
ناکافی)؛ لوپ‌بک و بازه‌های خصوصی دست‌نخورده می‌مانند چون `127.0.0.1:1819`
تشخیصی‌ترین رشتهٔ کل لاگ است؛ و خلاصه سقف حجم دارد.

**فهرست مجاز مدل، در چهار نقطه.** `models.list` روی یک کلید رایگان بیش از سی
مورد برمی‌گرداند که بخش بزرگی‌شان به اولین پرسش واقعی ۴۲۹ می‌دهند. فیلترکردن
فقط پاسخ تازه کافی نیست: فهرست کَش‌شدهٔ استارتاپ، انتخاب پیش‌فرض، و شناسه‌ای که
به `generateContent` می‌رسد هم از همان فهرست می‌گذرند.

**مدل نمی‌تواند تنظیمات امنیتی را بنویسد.** مشاور تنظیمات JSON برمی‌گرداند —
از یک API شبکه، در برنامه‌ای که دربارهٔ کلید قطع، گارد نشتی و اعتبارنامه‌های
Zero Trust حرف می‌زند. پرامپت دفاع نیست؛ پرامپت یک درخواست است. پس فقط کلیدهای
`WRITABLE` اعمال می‌شوند، کلید ناشناخته رد **و لاگ** می‌شود، `accessSecret` و
`accessToken` عمداً در فهرست نیستند، و نوع و بازه پیش از `normalize` بررسی
می‌شود. فهرست از خودِ `ConnectionProfile` مشتق شده و از نام‌های اندروید کپی
نشده — فهرست کپی‌شده بدترین شکل شکست را داشت: هر پیشنهاد بی‌صدا رد می‌شد و به
کاربر «اعمال شد» گفته می‌شد.

### چت، یک صفحهٔ مستقل با تمامِ امکاناتِ موبایل

گفت‌وگو یک تبِ جدا در ریلِ ناوبری است (`src/views/chat.js`) و نه کارتی در صفحهٔ
تنظیماتِ دستیار. دلیلِ ساختاری: گفت‌وگو تنها چیزی در این برنامه است که *رشد
می‌کند*، و یک لاگِ اسکرول‌شونده داخل صفحه‌ای که خودش اسکرول می‌شود دو اسکرولِ
تودرتو می‌سازد که کاربر همیشه اشتباهی بیرونی را می‌چرخاند.

امکانات: چهار پرسشِ آماده روی صفحهٔ خالی، کپیِ پاسخ، ویرایشِ پیامِ فرستاده‌شده،
حذفِ یکی یا چند پیام، «تلاش مجدد» روی پیامی که نرفته، و «توقف».

سه تضمینِ رفتاری که ارزش نوشتن دارند:

- **پرسش پیش از پاسخ روی صفحه است.** حبابِ کاربر روی همان رشتهٔ فرمان و *پیش از*
  رفتن به شبکه نشانده می‌شود (`append_user_message`)، بعد snapshot منتشر می‌شود،
  بعد درخواست می‌رود. ترتیب خودش تضمین است و نه یک تأخیر: اگر انتشار با نشاندنِ
  حباب مسابقه بدهد، کاربر متن را می‌فرستد و تا رسیدنِ پاسخ یک جعبهٔ خالی می‌بیند.
- **شکست یک حبابِ قابلِ «تلاش مجدد» است**، نه یک خط در نوارِ خطا. `sourcePrompt`
  روی خودِ شکست ذخیره می‌شود و با راه‌رفتنِ عقب در فهرست پیدا نمی‌شود: پس از یک
  ویرایش یا حذف، حبابِ بالای یک شکست لزوماً همان پرسش نیست، و فرستادنِ پیامِ
  اشتباه از نبودنِ دکمه بدتر است. جملهٔ خطا از `errorKind` ترجمه می‌شود و متنِ
  خامِ گوگل زیرش به‌عنوان جزئیات می‌ماند.
- **«توقف» صادق است.** درخواستِ HTTP روی رشتهٔ blocking لغو نمی‌شود، پس یک
  شمارندهٔ `epoch` هست؛ کارگر پیش از نوشتنِ پاسخ آن را می‌سنجد و اگر عوض شده باشد
  پاسخ را می‌اندازد. `clear_chat` هم epoch را بالا می‌برد، وگرنه پاسخِ در راه در
  گفت‌وگوی خالیِ بعدی ظاهر می‌شد.

**هیچ پیامی بی‌تأیید حذف نمی‌شود.** سطلِ روی هر حباب، حذفِ گروهی و پاک‌کردنِ کلِ
گفت‌وگو، هر سه از یک دیالوگ رد می‌شوند که تعداد را در *خودِ* پرسش می‌گوید — این
تفاوتِ «تأییدِ یک حذف» و «تأییدِ این حذف» است. گفت‌وگو نه undo دارد، نه سبد
بازیافت، نه نسخهٔ سمت سرور. دیالوگ از آنِ خودِ برنامه است و `window.confirm`
نیست: پنجرهٔ سیستمیِ WebView2 راست‌به‌چپ نمی‌شود و در رابط فارسی «OK / Cancel»
انگلیسی نشان می‌داد.

پایکِ **«متوجه نشدید؟ از دستیار بپرسید»** زیرِ حبابِ ✨ — و فقط زیر یک پاسخِ موفق،
چون زیرِ خطا این پرسش بی‌معناست — عنوانِ تنظیم *و خودِ توضیحی که کاربر خوانده* را
به چت می‌برد، تا پرسش «این را ساده‌تر بگو» باشد و دستیار همان توضیح را از صفر
تولید نکند. اسلات یک‌بارمصرف است: بازدیدِ دومِ صفحه پرسش را دوباره نمی‌فرستد.

### اعمالِ تنظیمات از دلِ گفت‌وگو

کاربر می‌تواند تغییری را بخواهد و دستیار آن را *پیشنهاد* کند. پاسخ با کارتی
می‌آید که هر تغییر را به شکلِ `تنظیم: قدیم → جدید` با دلیلِ یک‌خطیِ خودِ مدل نشان
می‌دهد، و یک دکمهٔ «اعمال» دارد. «اعمالِ ۴ تغییر» رضایت نیست؛ دیدنِ مقدارِ قدیم و
جدید هست.

مدل تغییرات را در یک code fence با نامِ اختصاصیِ `aether-apply` برمی‌گرداند
(`ai_prompts::split_chat_reply`). نامِ اختصاصی و نه ` ```json `: پاسخِ چت می‌تواند
به دلایلِ دیگری هم JSON داشته باشد — کاربر پرسیده «فایل پیکربندی WireGuard چه
شکلی است؟» — و برداشتنِ آن به‌عنوان پیشنهادِ تنظیمات، یک دکمهٔ «اعمال» روی چیزی
می‌ساخت که هرگز پیشنهاد نبود. بلوک از متنِ حباب **بریده** می‌شود تا کاربر JSON
خام نبیند.

دو قاعده این را ایمن می‌کنند:

- **اعتبارسنجی پیش از رسم.** پیشنهاد روی یک *کپی* از پروفایل اجرای خشک می‌شود
  (`vet_changes`)؛ فقط چیزی که واقعاً می‌نشیند دکمه می‌گیرد. تغییری که نگهبان رد
  می‌کند نباید اول به شکلِ دکمه دیده شود و بعد با زدنش هیچ اتفاقی نیفتد. در لحظهٔ
  زدنِ دکمه **دوباره** اعتبارسنجی می‌شود، چون بین آن دو لحظه کاربر می‌توانسته خودش
  تنظیمات را عوض کند.
- **مرزِ چت باریک‌تر از مرزِ مشاور است.** `chat_writable()` زیرمجموعهٔ
  `writable()` است و این‌ها عمداً بیرون‌اند: `upstream` (یک پراکسیِ بالادستِ
  نوشتنی یعنی «همهٔ ترافیک این کاربر از میزبانی که من می‌گویم»)، `routeDirect` و
  `routeBlock` (یک قاعدهٔ `direct` یک دامنه را از تونل بیرون می‌برد؛ «bank.example.com
  را direct کن» شکلِ یک نکتهٔ سرعتی دارد و یک لو‌رفتنِ هویت است)، `manualPeer` و
  `manualRange` (سنجاق‌کردنِ تونل به لبه‌ای که مهاجم انتخاب کرده)، `backend`
  (رفتن به Aether خالی خودِ هوش مصنوعی را خاموش می‌کند، پس یک پیشنهادِ بد قابلیت
  را از درونِ همان گفت‌وگو غیرقابلِ تعمیر می‌کرد)، `splitMode`، `lanShare`،
  `gateway`، `killSwitch` و هر فیلدِ Access. نوشتنی‌ها همان تنظیمِ ترابردند:
  پروتکل، `scanMode`، `noize`، MTU، `keepalive`، `fragment`، `ech`,
  `masqueHttp2`، `quickReconnect`، `reconnectAttempts`، `ipv6Protection`،
  `routeSniff`، `ipVersion` و `dns`.

نوشتن از همان یک دروازهٔ `set_profile` کنترلر رد می‌شود — همان چیزی که به دیسک
می‌نویسد، `settings_rev` را بالا می‌برد و تصمیم می‌گیرد نشستِ در جریان بازپیکربندی
شود. حباب فقط پس از یک نوشتنِ **موفق** «اعمال‌شده» علامت می‌خورد: یک تیکِ سبز روی
تغییری که به دیسک نرسیده، دروغ است.

**پروفایل یک مرجع دارد و آن سمت Rust است.** رابط کاربری پروفایل را در استارتاپ
یک بار می‌خواند، و `set_profile` **کلِ** پروفایل را می‌گیرد و نه یک تغییر. تا وقتی
هر نوشتنی از خودِ رابط شروع می‌شد این بی‌ضرر بود، ولی حالا Rust هم می‌نویسد؛ پس دو
چیز لازم است و هر دو هست: نوشتنِ سمتِ Rust با رخدادِ `aether://profile` اعلام
می‌شود تا صفحه‌ها همان لحظه مقدارِ تازه را نشان دهند، و `saveProfile` پیش از
نوشتن پروفایل را دوباره **می‌خواند** و تغییر را روی همان می‌نشاند. دومی به رسیدنِ
رخداد تکیه نمی‌کند، چون یک رخدادِ ازدست‌رفته وگرنه به این ترجمه می‌شد: کاربر تغییرِ
دستیار را اعمال می‌کند، بعد یک تنظیمِ بی‌ربط را عوض می‌کند، و شیءِ کهنه تغییرِ
دستیار را بی‌صدا برمی‌گرداند. `tests/profile-sync.mjs` هر دو نیمه را می‌سنجد.

پس از اعمال، یک پنجره می‌گوید تنظیمات تونل هنگام راه‌اندازی به موتور داده می‌شود و
باید یک‌بار قطع و وصل شود. این جمله در یک پنجره است و نه در یک خطِ کم‌رنگ داخل
کارت، چون مهم‌ترین جملهٔ کلِ قابلیت — اینکه چیزی که کاربر همین حالا تأیید کرده هنوز
فعال نیست — نباید چیزی باشد که می‌شود از آن رد شد.

## ۲) منوی تنظیمات، همان‌که در تصویر a2 است

ناحیهٔ تنظیمات از نو ساخته شد: هاب با گروه‌ها و ردیف‌هایی که آیکن، عنوان،
زیرعنوان، **مقدار فعلی**، شِوران و ✨ دارند؛ کلیک روی ردیف زیرصفحه را باز
می‌کند؛ ردیف قرمزِ بازنشانی یک کنش جدا با تأیید است.

نکتهٔ مهندسی: هاب و زیرصفحه‌ها از **یک** تعریف مشترک ساخته می‌شوند
(`renderAdvanced(sections)` روی بخش‌های نام‌دار). صفحهٔ مسطح «پیشرفته» حذف شد،
وگرنه دو مسیرِ UI برای یک تنظیم می‌ماند و رفتار ذخیره‌سازی بین آن دو واگرا
می‌شد. ردیف با `div role="button"` ساخته می‌شود، چون ✨ خودش یک دکمه است و دکمه
داخل دکمه DOM نامعتبر است.

آزمون jsdom (`tests/settings-smoke.mjs`) این‌ها را می‌سنجد: تعداد ✨ برابر تعداد
برچسب‌ها، مقادیر خوانا (نه `MANUAL_RANGE`)، رسیدن تغییر به انبار پروفایل، و
اینکه با دروازهٔ بسته حبابِ ✨ **مدل را صدا نمی‌زند**.

## ۳) هستهٔ ۱.۸.۰ → ۱.۹.۰ — merge، نه کپی

کپی‌کردن سادهٔ هستهٔ موبایل، پچ‌های مخصوص دسکتاپ را پاک می‌کرد. پس merge
سه‌طرفهٔ واقعی انجام شد: `native/aether/.upstream-baseline` (بکرِ ۱.۸.۰) به‌عنوان
مبنا، نسخهٔ پچ‌خوردهٔ دسکتاپ، و آپ‌استریم ۱.۹.۰. نتیجه: ۲۰ تعارض، همه حل، صفر
باقی‌مانده؛ و هستهٔ merge‌شده `cargo check` بی‌خطا و **۲۶۵ آزمون** خودش را رد
می‌کند.

**پچی که کنار گذاشته شد:** `masque_h2.rs`. آپ‌استریم مسیر ارسال HTTP/2 را با
`pump_outbound` و دسته‌بندی کپسول‌ها از نو نوشت — همان کاری که پچ ۱.۲.۳ می‌کرد.
نگه‌داشتنِ نسخهٔ ما یعنی نگه‌داری یک فورک از کدی که حالا مالکش آپ‌استریم است، پس
فایل از `PATCHED_FILES` و از baseline بیرون رفت.

**پچی که ماند و حالا علامت‌دار است** (`AETHER-APP-PATCH`، تا ارتقای بعدی بفهمد
این خط مالِ برنامه است نه آپ‌استریم): انتخاب CUBIC در smoltcp همراه ویژگیِ
جفتش `socket-tcp-cubic` (بی آن ویژگی، `AnyController::new()` به `NoControl`
می‌رسد و کنترل ازدحام بی‌صدا از دست می‌رود)، سقفِ صفِ تحویل بسته در `lib.rs`، و
تفکیک `SO_RCVBUF`/`SO_SNDBUF` در `sysprofile.rs`.

`CoreCaps::for_version` با `>=` مقایسه می‌کند، پس ۱.۹.۰ همهٔ قابلیت‌های ۱.۵.۰ و
۱.۷.۰ را خودکار روشن نگه می‌دارد؛ هیچ نسخه‌ای در کد سخت‌کد نشده و
`CORE_VERSION` در زمان اجرا از کنار `aether.exe` خوانده می‌شود.

## ۴) اصلاح: «بازهٔ آدرس» هیچ‌وقت کار نمی‌کرد

این باگ در بازرسی همین ارتقا پیدا شد، با مقایسهٔ ماشینیِ متغیرهایی که برنامه
می‌فرستد با آن‌هایی که هسته می‌خواند.

حالت اندپوینت *Manual range* سه متغیر `AETHER_SCAN_CIDRS`،
`AETHER_MASQUE_CIDRS` و `AETHER_WG_CIDRS` را می‌فرستاد و **هیچ نسخه‌ای از
هسته** — نه ۱.۹.۰، نه ۱.۸.۰ — هیچ‌کدام را نمی‌خواند: کاربر `162.159.192.0/24`
می‌نوشت و اسکن همان بازه‌های توکار خودش را جارو می‌کرد. کامنت مخزن موبایل
نشان می‌دهد این خواندن، پچِ خودِ برنامه بوده و نه قابلیت آپ‌استریم؛ در دسکتاپ
هرگز اعمال نشده بود.

پچ `AETHER-APP-PATCH scan-cidrs` در `prober.rs`، `wg_prober.rs` و
`wireguard.rs`:

* متغیر مخصوصِ پروتکل مقدم است، `AETHER_SCAN_CIDRS` پشتیبانِ هر دو؛
* ورودی کاربر اعتبارسنجی می‌شود. `10.0.0.0/64` رد می‌شود، چون
  `parse_cidr_v4` هر عددی را که در `u8` جا شود می‌پذیرد و بعد در
  `sample_cidr_v4` به یک آدرس تنها فرومی‌پاشد — بازهٔ غلط، بی هیچ پیامی، به یک
  میزبان تبدیل می‌شد؛
* آدرس تنها بازهٔ یک‌میزبانه می‌شود (`/32` یا `/128`) و ترتیب نوشتهٔ کاربر حفظ
  می‌شود — `prioritize` روی بازهٔ دستی اعمال نمی‌شود، چون خودِ کاربر گفته چه
  چیزی اول بیاید؛
* **دانه‌های توکارِ بیرون از بازه پروب نمی‌شوند.** دانه‌ها اول از همه پروب
  می‌شوند؛ اگر دست‌نخورده می‌ماندند، پین‌کردن بازه بی‌معنی بود و تونل با احتمال
  زیاد روی آدرسی بسته می‌شد که کاربر نخواسته. دانهٔ *داخلِ* بازه می‌ماند تا
  سرعت راه‌اندازی حفظ شود؛
* آدرس v4 که در آدرس v6 وارپ جاسازی می‌شود هم از بازهٔ کاربر گرفته می‌شود،
  وگرنه سمت v6 از بازه بیرون می‌زد؛
* اگر هیچ تکهٔ معتبری نماند، رفتار توکار برمی‌گردد. اسکنِ خالی یعنی «هرگز وصل
  نشو»، و کاربری که بازه را غلط تایپ کرده انتظارِ آن را ندارد.

سه آزمون واحد این‌ها را قفل می‌کنند: پذیرش/رد ورودی، جانشینی بازه و بُرشِ
دانه‌ها، و عضویت در بازه که روی **پیشوند** حساب می‌شود نه روی متن.

## ۵) رابط کاربری دسکتاپ

**ظرفِ صفحه‌ها عرض دارد.** `<div id="view">` — تنها فرزندِ `<main>` و ظرفِ هر
صفحه — یک flex item است و بدون قاعدهٔ عرض به عرضِ محتوایش جمع می‌شود: اندازه‌گیریِ
واقعی در پنجرهٔ ۹۶۰ پیکسلی **۲۷۶ پیکسل** داد. `width: 100%` روی `.view` نجاتش
نمی‌داد، چون ۱۰۰٪ِ همان ظرفِ جمع‌شده بود، و `--content-max: 560px` عملاً هیچ‌وقت
به کار نمی‌آمد. حالا `#view` خودش `width: 100%`، `min-width: 0` و
`max-width: var(--content-max)` دارد.

**کارت اتصال متناسبِ کارتِ موبایل است**: `max-width: 430px` و `margin-inline: auto`.
دایره‌های نوارِ پینگ سقفِ عرض دارند (`PING_BAR_MAX_W = 11`) و ردیف‌ها مرکزچین‌اند —
با یک کارتِ عریض، ۲۶ میله کشیده می‌شوند و ریتمِ پینگ بی‌معنا می‌شود. نورِ دورِ کارت
**پادساعتگرد** می‌چرخد، مثل موبایل.

**فارسی جداگانه سنجیده می‌شود.** متریکِ فونت فارسی همان چیدمان را ۱۶ تا ۱۹ پیکسل
بلندتر می‌سازد — نه از سرِ شکستنِ خط. در کوچک‌ترین پنجرهٔ مجاز (۹۶۰×۶۴۰) همین
مقدار برای بیرون‌زدنِ کارت کافی است. در پلهٔ `max-height: 700px` ریتمِ کارت تنگ
می‌شود و کلِ `hero` می‌رود — نام برنامه دو سانت بالاتر در نوار عنوانِ خودِ پنجره
نوشته شده، پس حذفش چیزی از کاربر نمی‌گیرد، برخلاف هر ردیفی از خودِ کارت.

یک قاعدهٔ سراسری `[hidden] { display: none !important; }` هم هست: صفتِ `hidden`
فقط یک `display: none`ِ پیش‌فرضِ مرورگر است و زیر هر قاعدهٔ `display` صریح گم
می‌شود — مثلاً `.connect__icon` که `display: grid` دارد.

## چه چیزی عوض **نشد**

پروفایل‌های ذخیره‌شده دست‌نخورده بارگذاری می‌شوند. تا کلیدی وارد نشود لایهٔ
هوش مصنوعی خاموش است. هیچ تنظیم موجودی پیش‌فرض، نام یا معنایش را عوض نکرده و
هیچ فلگ یا متغیر محیطیِ موتور حذف نشده است.

## آزمون‌ها

- `tests/chat-smoke.mjs` — ۶۶ سنجه روی *رفتار* و نه ظاهر: معیارِ درستی، فرمانی
  است که به Rust می‌رود. دکمه‌ای که وجود دارد ولی `ai_apply_changes` را با
  شناسهٔ درست صدا نمی‌زند، از نبودنش بدتر است. شاملِ «انصراف هیچ چیزی حذف نمی‌کند»
  و تله‌ای روی `window.confirm` تا بازگشتِ پنجرهٔ سیستمی بی‌صدا نماند.
- `tests/profile-sync.mjs` — پروفایل یک مرجع دارد: رخدادِ سمتِ Rust کپیِ رابط را
  جایگزین می‌کند، و `saveProfile` پیش از نوشتن می‌خواند تا ویرایشِ یک تنظیم،
  تنظیماتِ دیگر را با مقدارِ کهنه بازنویسی نکند.
- `tests/settings-smoke.mjs`، `tests/assistant-smoke.mjs`، `tests/ui-smoke.mjs`،
  `tests/css-classes.mjs` — منوی تنظیمات، صفحهٔ دستیار، و اینکه هر کلاسِ
  استفاده‌شده در CSS تعریف شده باشد.
- `tests/glow-direction.mjs` — جهتِ چرخشِ نور **اندازه‌گیری** می‌شود و حدس نیست:
  با مساحتِ علامت‌دار که پیمایشِ مسیر پادساعتگرد است، و با دو `phase` که باند در
  جهتِ همان مسیر جلو می‌رود. شرطِ دوم لازم است چون `lineDashOffset` دash را به عقب
  می‌لغزاند؛ بدونِ آن، عوض‌کردنِ مسیر هیچ اثری روی جهتِ دیده‌شده ندارد.
- `tests/layout-desktop.mjs` — بستهٔ واقعیِ vite در یک مرورگر واقعی، در دو اندازه
  × دو زبان: سند اسکرول ندارد، کارت کامل داخل کادر است، `main` سرریز ندارد؛
  به‌علاوه یک بازرسیِ پیکسلی روی سه ردیفِ آخرِ تصویر، چون `getBoundingClientRect`
  یک بار «۸ پیکسل جا دارد» گفت در حالی که ردیفِ دایره‌های پینگ از پایین بریده بود.
  jsdom هیچ‌کدام را نمی‌تواند بگوید: چیدمان را اندازه نمی‌گیرد. بی‌مرورگر با SKIP
  رد می‌شود و بیلد را نمی‌شکند.
- سمت Rust، ۴۱ آزمون واحد در `ai_patch.rs`، `ai_prompts.rs` و `ai_session.rs` —
  از جمله اینکه کلیدهای حذف‌شده از مسیر چت رد می‌شوند *و* در همان حال مسیر مشاور
  آن‌ها را می‌پذیرد (تا ثابت شود ردشدن از فهرست آمده و نه از بی‌اعتباریِ مقدار)،
  و اینکه حبابِ کاربر همان لحظهٔ برگشتِ `append_user_message` در تاریخ هست.
- `scripts/preflight-local.sh` (`npm run preflight`) — همان گام‌های Preflight در
  CI که بی‌ویندوز هم اجرا می‌شوند: فایل‌های لازم، اعتبارِ JSONها، یکی‌بودنِ نسخه
  در پنج جا، نبودِ مسیرِ کدِ حالتِ پراکسی، بیلد وب، `npm test` و `npm run test:layout`.

## بازرسی پیش از انتشار

`cargo test` هستهٔ merge‌شده (۲۶۵ آزمون)، آزمون‌های jsdom رابط، بیلد وب،
`cargo check --target x86_64-pc-windows-gnu`، بیلد ریلیز Tauri روی x64 و x86،
verify مانیفستِ جاسازی‌شده، نصب و حذف بی‌صدای نصاب، پاک‌سازی کلید قطع، و آزمون
دستی نشت WebRTC/DNS/IPv6.
