# Aether Desktop 1.2.3

دو تغییر، و هر دو ریشه‌ای.

## ۱) بک‌اند زنجیره‌ای `Aether → Psiphon`

قابلیت نسخهٔ موبایل (`AetherMobile 1.2.8`) با **همان متد** به ویندوز آمد.

```
stage 1   موتور اِتِر  → SOCKS5 127.0.0.1:1819     (هنوز هیچ مسیر داده‌ای)
stage 2   Psiphon      → SOCKS5 127.0.0.1:1825     از راه 1819 dial می‌کند
سپس      پل + پروکسی سیستمی → 127.0.0.1:1825      خروجی = Psiphon
```

**چرا:** خروجی‌های اِتِر/WARP لبه‌های anycast کلادفلر هستند و مجموعهٔ بزرگی از
مقصدها آن‌ها را رد می‌کنند یا نمای دیگری سرو می‌کنند — تونل بالاست و سایت باز
نمی‌شود. زنجیره **خروجی** را با یک IP هاستینگ عادی عوض می‌کند و ترابرد
مبهم‌سازی‌شدهٔ اِتِر را روی هاپ اول نگه می‌دارد؛ همان هاپی که باید از شبکهٔ محلی
جان سالم ببرد.

### آنچه عیناً از موبایل پورت شد
* کلیدهای کانفیگ Psiphon، از جمله `UpstreamProxyUrl` که باعث می‌شود **هر**
  اتصال Psiphon (حتی واکشی فهرست سرور) از استیج ۱ برود، پس زنجیره نمی‌تواند یک
  dial مستقیم لو بدهد.
* دو پاس برقراری: پاس اول کشور کاربر، پاس دوم بدون فیلتر + datastore تازه.
  `EgressRegion` فیلتر **سخت** است و بدون این، انتخاب یک کشورِ بی‌سرور یعنی
  «Connecting» تا ابد.
* دنبال‌کردن پورتی که Psiphon **واقعاً** گرفته (`ListeningSocksProxyPort`)،
  به‌جای دیکته‌کردن آن.
* دروازهٔ استیج ۱ (`run_proxy_stage`): پورت، دست‌دادن SOCKS5، و یک TCP واقعی —
  عمداً بدون geo lookup، وگرنه نشانِ پرچم کشور **هاپ اول** را نشان می‌داد.
* پنجرهٔ گریس ۱۵۰ ثانیه‌ای (`EXTERNAL_GRACE_MS`) به‌جای ۹۰ ثانیه، چون نشست
  زنجیره‌ای گرم‌شدن هر دو هاپ را می‌پردازد. رد‌کردن نشستی که فقط کُند است
  بدترین نتیجهٔ ممکن است.
* واچ‌داگ سرورِ فیلترکننده (`psiphon_health.rs`) با همان آستانه‌ها، همان فهرست
  سیاه، همان هدایت منطقه، و همان پنجرهٔ مهلت چرخش که نمی‌گذارد یک جابه‌جایی
  عمدی «مرگ» خوانده شود.

### آنچه عمداً پورت **نشد** — و چرا این خبر خوبی است
در اندروید یک لایهٔ سوم هست: `PsiphonSocksFront` روی ۱۸۲۵ با udpgw، لِین
اختصاصی DNS، حمل QUIC و سرکوب AAAA. آن لایه فقط برای این وجود دارد که
tun2socks اندروید همهٔ UDP و DNS را با `UDP ASSOCIATE` می‌برد و پروکسی محلی
Psiphon فقط `CONNECT` می‌فهمد.

مسیر دادهٔ ویندوز پروکسی سیستمی WinINET → پل `share.rs` است: از بنیاد
**TCP-only و IPv4-only**، و فقط `CONNECT` می‌خواهد. پس Psiphon مستقیم روی ۱۸۲۵
می‌نشیند و آن لایه وجود ندارد.

و این تصادفی نیست که همان شکل «درست» است. در `PSIPHON_APP_CONNECTIVITY.md`
موبایل، سرنخ قطعی این بود که با USB-tether گوشی به لپ‌تاپ،
`gemini.google.com` و `aistudio.google.com` **بلافاصله** باز می‌شدند — چون آن
مسیر پروکسیِ TCP-only/IPv4-only همان دو چیزی را حذف می‌کرد که روی گوشی خراب
بود. مسیر دادهٔ ویندوز **همان مسیر لپ‌تاپ** است:

* **QUIC**: مرورگر پشت پروکسی برای مقصدها HTTP/3 نمی‌زند و تمیز به TCP
  برمی‌گردد، و `leakguard` هم UDP خروجی مرورگر را می‌بندد. هیچ دیتاگرام
  UDP/443 ای در سیاه‌چاله گم نمی‌شود که Gemini/ChatGPT آن را «اینترنت نداری»
  بخواند.
* **AAAA/IPv6**: مسیر داده لوپ‌بک IPv4 است و هیچ آدرس IPv6 ای به رزولور ویندوز
  اعلام نمی‌شود، پس Happy Eyeballs هیچ‌وقت AAAA ای را ترجیح نمی‌دهد که خروجی
  Psiphon نمی‌تواند بگیرد — همان چیزی که روی اندروید خطای «بلاک کشور» می‌ساخت.
* **DNS**: نام‌ها با `ATYP=DOMAIN` آن‌طرفِ Psiphon حل می‌شوند، نه اینجا.

## ۲) هستهٔ اِتِر: 1.7.0 → 1.8.0

`native/aether` روی تگ 1.8.0 نشست. `cli.rs`، `config.rs` و `consts.rs` بین دو
نسخه **بایت‌به‌بایت یکسان**اند، یعنی هیچ فلگ یا متغیر محیطی‌ای عوض نشده و
`CoreCaps::for_version` (که با `>=` کار می‌کند) همهٔ قابلیت‌های 1.7 را روشن نگه
می‌دارد. مخزن هیچ پچ محلی روی هسته نداشت (`.upstream-baseline` با سورس یکی
بود)، پس ارتقا یک جای‌گذاری تمیز بود و `BASELINE` اسکریپت sync هم به 1.8.0
رسید.

## نکتهٔ ارتقا
پروفایل‌های ذخیره‌شدهٔ قبلی دست‌نخورده کار می‌کنند: `backend` با
`#[serde(default)]` روی `AETHER` می‌افتد، یعنی رفتار پیش‌فرض عیناً ۱.۲.۲ است.
بک‌اند زنجیره‌ای یک انتخاب آگاهانه در «پیشرفته → ترابرد» است.

## پیوست — رفع خطای بیلد این نسخه

مرحلهٔ `Unit tests (engine contract parity with Android)` روی هر دو معماری با
پنج خطای `E0425/E0433: cannot find type TransportBackend` می‌افتاد
(`src-tauri/src/profile.rs` خطوط ۱۸۹، ۲۹۲، ۲۹۳، ۲۹۹، ۳۶۴).

**ریشه:** در پورت بک‌اند زنجیره‌ای، فیلد `ConnectionProfile::backend` و هر پنج
مرجع آن اضافه شده بود، ولی **خودِ `enum TransportBackend`** هرگز به
`profile.rs` نیامد — یعنی همان `model/TransportBackend.kt` سمت اندروید جا افتاد.
`state.rs` هم `backend.is_chained()` و `backend.pipeline_label()` را صدا می‌زد
که وجود نداشتند. این خطای حل‌نام است، پس مرحلهٔ بعدی (`Build Aether (Tauri,
release)`) هم قطعاً با همان پنج خطا می‌افتاد؛ مرحلهٔ تست فقط زودتر به آن رسید.

**رفع:** `TransportBackend` با دو حالت `AETHER` / `AETHER_PSIPHON` (عیناً کدهای
`BACKENDS` در `src/views/advanced.js`) به همراه `is_chained()` و
`pipeline_label()` در `profile.rs` تعریف شد. قرارداد سیم و دیسک دست‌نخورده
است: پیش‌فرض `AETHER`، پروفایل‌های قبلی همان‌طور بار می‌شوند، و هیچ فلگ یا
متغیر تازه‌ای به موتور نمی‌رود. شش تست تازه همین قرارداد را قفل می‌کنند.

نسخه عوض نشده: `1.2.3` در `tauri.conf.json`، `package.json` و `aether.iss`.

سه هشدار باقی‌ماندهٔ کامپایلر هم پاک شد (import بی‌استفادهٔ `IpVersion` در
`tun.rs`، بایندینگ بی‌استفادهٔ `exe` در `psiphon.rs::establish`، و `mut`
غیرلازم در `state.rs`).

---

## پیوست ۲ — رفع ریشه‌ای: حالت ترکیبی `اِتِر + سایفون` روی ویندوز کانکت نمی‌شد

**گزارش:** روی نسخهٔ موبایل حالت ترکیبی درست کار می‌کند، ولی روی
`AetherDesktop-1.2.3` هر بار کانکت نمی‌شود. لاگ برنامه نشان می‌دهد که در حالت
`Aether only` همه‌چیز کامل بالا می‌آید و خودآزما پاس می‌شود
(`exit 104.28.214.xxx IR`)، ولی در حالت `Aether → Psiphon`:

```text
1788501253634 I/diag: Stage 1 is a working SOCKS5 proxy — starting stage 2.
1788501253636 I/psiphon: Starting Psiphon (exit=Automatic, socks=1825, upstream=socks5://127.0.0.1:1819)
1788501259510 W/psiphon: Error: error getting listener IP: psiphon.(*Controller).Run#424:
                         common.GetInterfaceIPAddresses#38: route ip+net: no such network interface
1788501259510 D/psiphon: Info: shutdown by controller
1788501259734 W/state:   The Psiphon stage failed: The Psiphon stage exited before it established a tunnel
```

یعنی استیج ۱ (MASQUE) بی‌عیب بود — تونل validate شده و SOCKS5 روی ۱۸۱۹ باز بود —
و استیج ۲ حدود شش ثانیه بعد از استارت، **پیش از ساختن هیچ لیسنری**، تمام می‌شد.
نردبان Smart Auto بعد به `MASQUE سخت‌شده` و بعد `GOOL` و `WIREGUARD` می‌رفت و
همان‌جا هم می‌افتاد؛ چون علت هیچ ربطی به استیج ۱ نداشت.

### ریشه

در `spawn_child` این آرگومان فرستاده می‌شد:

```text
-listenInterface 127.0.0.1
```

با این نیت که «استیج ۲ فقط روی لوپ‌بک بشنود». ولی `ListenInterface` در
psiphon-tunnel-core یک **نام اینترفیس** می‌گیرد، نه یک آدرس IP. قرارداد
`Controller.Run` این است:

| مقدار `ListenInterface` | نتیجه |
|---|---|
| `""` (خالی) | `listenIP = 127.0.0.1` ← **همان چیزی که می‌خواستیم** |
| `"any"` | `listenIP = 0.0.0.0` |
| هر چیز دیگر | `common.GetInterfaceIPAddresses(<آن رشته>)` → `net.InterfaceByName` |

ویندوز اینترفیسی به نام `127.0.0.1` ندارد، پس `net.InterfaceByName` خطای
`route ip+net: no such network interface` می‌دهد، کنترلر `NoticeError` می‌نویسد
و `Run` **return** می‌کند. هیچ‌وقت نه لیسنر SOCKS5 ساخته می‌شد، نه notice
`ListeningSocksProxyPort` می‌آمد، نه `Tunnels` — و `establish` فقط می‌توانست
بگوید «فرآیند پیش از برقراری تونل تمام شد».

**چرا روی موبایل بود و نبود:** `transport/PsiphonTransport.kt` کتابخانهٔ
`psiphontunnel` را **بدون** `ListenInterface` صدا می‌زند و خود کتابخانه روی
لوپ‌بک می‌نشیند. پس این باگ فقط در پورتِ ویندوز متولد شد، نه در قابلیت.

### رفع

۱. آرگومان **حذف** شد، نه اینکه مقدارش عوض شود: پیش‌فرضِ خالی عیناً همان
   «فقط لوپ‌بک» است که هدف اصلی بود، پس سیاست امنیتی دست‌نخورده می‌ماند و یک
   وابستگی شکننده حذف می‌شود.
۲. ساختِ فهرست آرگومان‌ها به تابع خالص `child_args` منتقل شد و تست
   `stage_two_is_never_told_which_interface_to_bind` قفلش کرد، تا این آرگومان
   نتواند بی‌صدا برگردد.
۳. **زودتر و دقیق شکست خوردن.** `config_fault_hint` از جریان notice تشخیص
   می‌دهد که خطا پیکربندی/آرگومانی است (نه شبکه‌ای)، پیام قابل‌عملش را ثبت
   می‌کند، و `PsiphonTransport::start` بدون رفتن به پاس دوم برمی‌گردد.
   `state.rs` هم با `psiphon::is_config_fault` بلافاصله `fail` می‌کند، به‌جای
   `advance_or_fail`: پیمودنِ چهار پلهٔ نردبان وقتی هر پله قطعاً با همان خطا
   می‌افتد، ۹۰ ثانیه وقت کاربر را می‌گیرد و علت را هم پنهان می‌کند. شکست
   **شبکه‌ای** رفتار قبلی را نگه می‌دارد، چون آنجا تلاش دوباره واقعاً جواب
   می‌دهد.

### پیوست — پرچم کشورها در حالت ترکیبی

فیلد «کشور خروج» یک `<select>` بومی بود و `<option>` بومی نمی‌تواند SVG داشته
باشد، پس فهرست فقط اسم بود در حالی که `ExitRegions.kt` موبایل کنار هر ردیف
پرچم دارد. اموجی هم جواب نمی‌دهد: ویندوز فونت regional-indicator ندارد.
کنترل حالا یک `listbox` واقعی است با پرچم SVG درون‌خطی از `src/flags.js`،
کرهٔ زمین برای ردیف «خودکار»، ناوبری کامل کیبورد و تایپ‌اِهد. پرچم `MD`
(مولداوی) هم اضافه شد؛ تنها منطقهٔ خروجی بود که به کرهٔ زمین برمی‌گشت.

نسخه عوض نشده: `1.2.3` در `tauri.conf.json`، `package.json` و `aether.iss`.
